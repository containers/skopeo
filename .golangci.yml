version: "2"
linters:
  enable:
    - errorlint
  settings:
    staticcheck:
      checks:
        - all
        - -ST1000 # Incorrect or missing package comment.
        - -ST1005 # Incorrectly formatted error string.
  exclusions:
    presets:
      - std-error-handling
